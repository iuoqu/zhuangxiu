# 4F 方案 D · AI 出图（Vercel 部署说明）

一个带四位门禁码的小页面：选视角 → 改提示词 → 调 AI 出图 → 左右滑块和渲染底图对比。
出图引擎有两个可选：**OpenAI 的 gpt-image-1** 和**阿里云百炼的千问 qwen-image-edit**，
页面上逐次切换，见下面「两个引擎」。API key 和门禁码都只存在服务端环境变量里，**不会下发到浏览器**。

## 部署

1. 把这个仓库连到 Vercel（New Project → Import Git Repository）
2. **Root Directory 保持默认（留空 / `./`）** —— 页面就在仓库根目录，不要填子目录
3. Framework Preset 选 **Other**，Build Command 和 Output Directory **都留空**
   （`index.html` 在根目录直接当首页，`api/` 由 Vercel 自动识别成 Serverless Function，不需要构建）
4. Environment Variables（`PIN` 必配，两个引擎的 key 至少配一个）：

   | Key | Value | 说明 |
   |---|---|---|
   | `OPENAI_API_KEY` | `sk-...` | 你的 OpenAI key。**选 OpenAI 引擎时必需** |
   | `PIN` | 四位数字，如 `4816` | 门禁码 |
   | `DASHSCOPE_API_KEY` | 可选，`sk-...` | 阿里云百炼的 key。**选千问引擎时必需**；不配就只能用 OpenAI |
   | `ENGINE` | 可选，`openai` / `qwen` | 页面上没另选时用哪个引擎，不填是 `openai` |
   | `QWEN_MODEL` | 可选 | 不填默认 `qwen-image-edit-plus`，模型不存在时自动退到 `qwen-image-edit` |
   | `DASHSCOPE_BASE` | 可选 | 默认北京 `https://dashscope.aliyuncs.com`；新加坡地域填 `https://dashscope-intl.aliyuncs.com`。**两个地域的 key 不通用** |
   | `VISION_MODEL` | 可选，如 `gpt-4.1` / `qwen-vl-max` | 读风格用的视觉模型，不填按端点取默认值 |
   | `VISION_BASE` | 可选 | 读风格走哪个端点，默认 OpenAI；填 `https://dashscope.aliyuncs.com/compatible-mode/v1` 就换成千问 |
   | `GITHUB_TOKEN` | 可选，`github_pat_...` | **配了它出图才会自动存进仓库**，见下节 |
   | `GITHUB_REPO` | 可选，如 `iuoqu/zhuangxiu` | 存到哪个仓库 |
   | `GITHUB_BRANCH` | 可选 | 默认 `main` |
   | `SAVE_DIR` | 可选 | 默认 `产出` |

5. Deploy。之后改环境变量要 **Redeploy** 才生效。

命令行部署同理：

```bash
npm i -g vercel
cd <仓库根目录>
vercel                      # 首次会问设置，Root Directory 用默认的 ./
vercel env add OPENAI_API_KEY
vercel env add PIN
vercel --prod
```

## 两个引擎：OpenAI 与千问

页面「引擎」那个下拉，每次生成都可以单独选：

| | 模型 | 环境变量 | 特点 |
|---|---|---|---|
| **OpenAI**（默认） | `gpt-image-1` | `OPENAI_API_KEY` | 有 `input_fidelity: high`，**几何保真是这条管线真正吃饭的家伙**；贵，high 画质一张一块多 |
| **千问** | `qwen-image-edit-plus` | `DASHSCOPE_API_KEY` | 便宜大概一半以上；支付宝就能充值，不用境外卡；几何保真度**得你自己实测** |

两边送进去的东西是同一套：**几何底图 → 风格参考图 → 线稿**，最多三张，次序固定。
千问是多图指令编辑，所以后端会在提示词前面自动加三行，点明哪张是几何、哪张是风格、哪张是线稿 ——
不加这三行，模型会把风格参考图的布局也一起搬过来。

**几个只在千问这条路上的取舍：**

- **不传 `size`。** 编辑类模型跟随输入图的尺寸，正好是要的 —— 底图 1536×1024 的取景不能被改。
- **关掉 `prompt_extend`。** 它会改写扩写提示词，而这里的提示词是逐条锁死的设计决定。
- **`watermark: false`。** 出图是要拿去比方案的，不能带水印。
- **「画质」那个下拉对千问无效**，选了千问它会自动置灰 —— low/medium/high 是 gpt-image-1 的档位。
- 千问返回的是**只活 24 小时的 URL**，后端当场取回来转成 base64，所以存仓库和页面显示都不受影响。

### 怎么横评

`产出/*.json` 里现在多记了 `engine` 和 `model` 两个字段，git 提交信息里千问出的图也会标「千问」。
**同一个底图、同一段提示词、两个引擎各跑一次**，再用页面上的左右滑块分别对着底图拉一遍，
按下面「出图后必须核这三条」逐条看几何有没有飘。这是决定默认用哪家的唯一靠谱办法 ——
别看单张观感，观感容易好，几何容易飘。

## 出图自动存进仓库

Vercel 的函数文件系统是**临时只读**的，写哪儿都留不住 —— 所以出的图默认只在浏览器里，
关掉就没了。配上 `GITHUB_TOKEN` ＋ `GITHUB_REPO` 之后，每次出图会**自动提交回仓库**：

```
产出/20260901-1018_01.jpg      出图本身（JPEG，质量 90）
产出/20260901-1018_01.json     这张图是怎么来的：视角、引擎、模型、底图、画质、完整提示词
```

一次生成 = 一个提交，图和元数据在同一个提交里。**任何一张图都能溯源到它的提示词**，
方便回头复现或对比。页面上会显示存到了哪个路径；存失败不影响出图，只提示原因。

**怎么办这个 token**：GitHub → Settings → Developer settings → Personal access tokens →
Fine-grained tokens，只勾这一个仓库，权限只给 **Contents: Read and write**，其余全不给。

**两个提醒**：

1. 每次保存都会往 `main` 推一个提交，**Vercel 默认会因此重新部署一次**。要避免的话，
   到 Vercel → Settings → Git → Ignored Build Step 填：
   `git diff --quiet HEAD^ HEAD -- . ':(exclude)产出'`
   意思是「只有产出目录变了就别重新构建」。
2. 图会永久留在 git 历史里。JPEG 一张约 200~400 KB，出几百张也就几十 MB，
   但**删了也不会缩小仓库**，心里有数。

## 底图：白模（默认）还是渲染图

页面最上面那一对单选：

| | 是什么 | 什么时候用 |
|---|---|---|
| **白模 · 吊顶 3.0 m**（默认） | 原设计的天花高度，只有几何和明暗，**没有任何材质、颜色、光照信息** | 日常用这个 |
| **白模 · 裸顶 4.28 m** | 开敞区不做吊顶，直接到结构板底；风管、桥架、喷淋主管外露 | **比选裸顶方案时必须用它** —— 净高差 1.28 m 是几何，AI 改不出来 |
| 渲染图 | Blender/Cycles 路径追踪成图，已带木地板、织物屏风等材质 | 想让 AI 尽量贴着既定材质走时用；缺点是它连「三维感」一起保留 |
| **自由取景** | 在浏览器里实时画的白模，**视角随便定** | 预设那六个视角不够用时 —— 走到任意位置、任意朝向、任意视高和焦距 |

### 自由取景怎么用

选「自由取景」后画布里就是方案 D 的三维模型（`models/*.json`，由
`分析/render.py --export` 导出，和预渲底图是同一份几何）：

| 操作 | 作用 |
|---|---|
| 拖动 | 转头 |
| 滚轮 | 前进 / 后退 |
| Shift ＋ 拖动 | 平移、升降 |
| W A S D | 走动 |
| 视高滑块 | 900 ~ 2400 mm（人视 1500~1600 最自然） |
| 焦距滑块 | 14 ~ 50 mm（室内一般 20~24） |
| 预设按钮 | 跳回那六个标准视角 |

点「生成」时，当前画面会按 1536×1024 抓下来当底图送给 AI，
所以**你在画布里看到什么构图，出图就是什么构图**。

选了裸顶做法时，取景模型会自动换成裸顶那一版（净高 4.28 m）。

选「白模 · 裸顶」时，下面的吊顶下拉会自动跟着切到「原建筑顶」，两边保持一致；
反过来只选了裸顶做法却还用 3.0 m 底图，页面会红字提醒。

之前家具老是方块感，一半原因就在这儿 —— 喂进去的渲染图本身长得像三维渲染，
AI 忠实地保留了那个观感。白模没有材质可保留，AI 只能照着提示词自己编，反而更自然。
选白模时提示词会自动加一句「参考图是无材质白模，只取几何，材质全部自己生成」。

白模不是渲的 —— 它由法线／深度／掩码三个数据通道在 numpy 里合成
（`分析/render.py --clay`），一张十几秒，而且保证中性，不会像渲染那样累积色偏。

## 设计选项 —— 真正要定的东西在这里选

页面上方那一排下拉，选完直接写进提示词，并且**优先级高于风格参考图**
（提示词里是 `FIXED DESIGN DECISIONS — these override anything above`）：

| 项 | 可选 |
|---|---|
| 吊顶 | 石膏板平顶（现状 3.0 m）／矿棉板 600×600／金属格栅／工位区吊顶岛＋通道裸顶／全裸顶 |
| 窗・遮阳 | 无遮阳／白色半透卷帘／浅灰遮光卷帘／垂直百叶／窗帘盒暗藏电动帘 |
| 灯具 | 嵌入筒灯（现状）／线形灯带顺工位带／悬吊线形灯／格栅灯盘 |
| 地面 | 木纹地胶 PF-01／方块地毯／浅灰地胶 PF-02／仿石纹大板砖 |
| 家具风格 | 原木简约／北欧／现代极简／工业风／商务稳重／科技公司 |
| 工位屏风 | 织物 1200／上部玻璃／无屏风／绿植隔断 |
| 整体色调 | 暖木中性／冷灰／米白通透／深色沉稳 |
| 人物 | 无人／少量办公人物 |

各视角的场景描述已经改成**只讲几何**（有什么、在哪儿），材质全部交给这些选项 ——
这样选项、风格图、默认描述三者不会互相打架。

> ⚠️ **「全裸顶」是个例外：它改的是几何，不是材质。**
> 底图的吊顶在 3.0 m，裸顶后净高是 4.28 m（结构板底，立面 DWG 实测）。
> AI 抬不了这个高度，只会把 3.0 m 的顶画成裸顶的样子，看着会矮一截。
> 真要比选裸顶方案，得改 `分析/render.py` 的 `H_CEIL` 重渲一版底图。
> 页面选中它时会弹这条提醒。

## 用风格参考图倒推风格

页面上「风格参考图」那一栏：传一张你想要的效果图或实景照，点**读取风格**，
后端把图交给视觉模型，读成两份东西 ——

- **英文材质段**：作为总体材质／光感参考并进提示词（上面的「设计选项」优先级更高，会压过它）
- **中文做法清单**：`部位 → 做法／颜色`，8~14 条，给设计师核对，也可以直接当材料表初稿

同时这张风格图**本身**也会作为第二张参考图一起送给出图引擎（第一张是几何底图），两个引擎都一样。
所以是「文字 ＋ 图」双通道给风格，几何仍然只认底图。

读完不满意，直接在下面的提示词框里改就行 —— 那段文字是可编辑的。

> 这一步花的是视觉模型的钱（很少），出图才是大头。同一张风格图读一次就够，
> 六个视角可以复用同一段描述，风格才统一。

## 关于那个四位门禁码 —— 请务必看完

**它挡得住路人，挡不住有心人。** 四位数字只有 1 万种组合。我做了两件事拖慢暴力破解：
服务端用 `timingSafeEqual` 做常数时间比较，猜错强制等 1.2 秒。但这只是拖延，不是防护。

**真正的护栏是下面两条，请一定做：**

1. **给这把 key 设消费上限**。OpenAI 在 Billing → Limits；百炼在费用中心设预算告警／限额。
   就算门禁被撞开，损失也封顶。
2. 给这把 key **单独建一个、只用于这个页面**，别用主账号的万能 key。两个引擎都是这条。

如果这页要对外发链接，更稳的做法是开 **Vercel 自带的 Deployment Protection**
（Settings → Deployment Protection → Password Protection / Vercel Authentication），
那是平台层的，比页面级门禁强得多。我这个门禁的定位是「同事之间图个方便」。

## 可能踩到的坑

| 现象 | 原因 / 处理 |
|---|---|
| 打开是 404: NOT_FOUND | 先访问 **`/api/health`**（这个不需要门禁码），它会告诉你函数看到的工作目录、根目录有哪些文件、两个环境变量配没配。<br>· health 也 404 → Vercel 根本没部署到这份代码：检查 Settings → General → **Root Directory 是否留空**（不要填 `ai-web`，那个目录已经没了），以及 Production Branch 是不是 `main`，然后 Redeploy<br>· health 正常但首页 404 → 检查 **Output Directory 是否留空**（填了任何值都会找错地方）<br>· health 显示环境变量缺失 → 变量加完要 **Redeploy** 才生效 |
| 生成到一半 504 | 函数超时。`vercel.json` 里写的是 `maxDuration: 300`，但**各套餐上限不同**，Hobby 可能吃不满。先把画质降到 `medium` 或 `low`；还不行就用本地脚本 `../分析/渲染/ai_openai.py` |
| `unknown parameter input_fidelity` | 后端已经会自动退回不带该参数重试一次，正常看不到。若持续报错，删掉 `api/generate.js` 里那一行即可 |
| 403 / 提示组织未验证 | gpt-image-1 需要 OpenAI 组织完成身份验证，到后台 Settings → Organization 做一下 |
| 选千问报 `服务端没配 DASHSCOPE_API_KEY` | 变量没加或加完没 Redeploy。`/api/health` 里能看到配没配 |
| 千问报鉴权失败 / 模型不存在 | 多半是**地域不对**：北京和新加坡各有各的 key 和地址，不能混用。key 是新加坡的就把 `DASHSCOPE_BASE` 设成 `https://dashscope-intl.aliyuncs.com`。模型名不认时后端会自动退到 `qwen-image-edit`，还不行就用 `QWEN_MODEL` 指定你账号里确实有的那个 |
| 选千问后「出几张」只出了 1 张 | 该模型不认 `n`，后端整组参数丢掉重试了一次。想要多张就多点几次生成 |
| 千问报提示词过长 | 百炼对提示词长度的限制比 OpenAI 紧。**别从末尾砍** —— 提示词最后那段是「不许改几何」的锁，砍了就白跑。要砍先砍「设计选项」里用不上的项，或者不勾风格图（它会往提示词里并一段英文材质描述） |
| 图出来了但工位数不对 | 见下一节 |

## 出图后必须核这三条

两个引擎**都没有 ControlNet**，都是「看着参考图重画一张」，没有重绘幅度、没有深度约束。
提示词里已经写死「不许增删移动任何家具、不许改工位数」，能压一部分，压不住全部：

1. **工位 50 个**（北区 3 带 × 9/9/7）—— AI 最爱自己加桌子
2. **主通道 1800、纵向支通道 1400** —— 常被画窄
3. **幕墙 4050 分格、落地玻璃无窗下墙** —— 常被画成有窗台的普通窗

页面上的左右滑块就是干这个用的：拖动对比渲染底图和 AI 出图，几何漂没漂一眼能看出来。

**要尺寸能当方案依据，走 ComfyUI + ControlNet depth** —— 条件图和参数在
`../分析/渲染/AI条件图/README.md`。这个页面的定位是快速看氛围和材质基调。

## 目录

```
<仓库根目录>              ← Root Directory 留空，就是这一层
  index.html              整个界面（无构建，原生 JS/CSS），部署后就是首页 /
  clays/01~06.png         白模底图 · 吊顶 3.0 m（默认，只有几何）
  bares/01~06.png         白模底图 · 裸顶 4.28 m（露结构与外露机电）
  models/model_clay.json  三维模型（吊顶 3.0 m），浏览器里自由取景用，约 76 KB
  models/model_bare.json  三维模型（裸顶 4.28 m）
  refs/01~06.jpg          Blender/Cycles 渲染底图（可选）
  lines/01~06.png         线稿（勾选「附加线稿」时一起送，实验性）
  api/generate.js         Serverless Function：校验门禁 → 调 OpenAI 或千问出图 → 返回 base64
  api/style.js            读风格：把参考图交给视觉模型 → 英文材质段 ＋ 中文做法清单
  api/health.js           部署自检（不需要门禁码，不返回密钥内容）
  vercel.json             maxDuration 300 ＋ includeFiles，保证函数读得到 refs/ 和 lines/
  package.json            只声明 type:module 和 Node 版本，没有依赖
  分析/                    平面分析、渲染脚本、报告 —— 与网页无关，不影响部署
```

> 页面和 `api/` 都放在仓库根目录 —— 这是 Vercel 的默认布局，所有设置保持默认即可，
> **不需要设 Root Directory，也不需要设 Output Directory**。
