# 4F 方案 · 怎么用

三个页面，同一个四位门禁码，同一个标签页里进一次就够。顶部导航条互相跳。

| 页面 | 干什么 | 什么时候用 |
|---|---|---|
| **排布** `/layout.html` | 排平面：填需求自动生成，或手动拉房间、刷工位带 | 要一版新排布 |
| **方案研究** `/plan.html` | 102／72／50／44 四版对照 ＋ **甲方 PLAN A／B**、房间尺寸表、疏散与机电复核 | 要依据、要跟甲方讲道理 |
| **AI 渲染** `/` | 选方案 → 选视角 → 出效果图 | 要给甲方看效果 |

---

## 一、排一版新平面

打开**排布**页。灰色的是**固定底板** —— 结构、幕墙、电梯、楼梯、厕所、客房，
怎么排都不动，点不动也选不中。其余都在你手里。

画布上还有一块**虚线蓝框「门厅进场」** —— 门厅出来那一片必须空着，
房间和工位都排不进去，不然人从电梯厅进来一头撞在会议室后墙上。
它的深度跟着**「主通道」**那一栏走（默认 1800），想要更气派的进场就把主通道调宽。

光有这个框还不够 —— 框自己空着，左边摆一间中会议室、下边摆一间洽谈室，
出了门厅照样是个死胡同。所以还会**真走一遍**：150 mm 栅格标出所有障碍
（房间、工位带、保留房间、柱、壳外），做距离变换，够宽的格子才算能走，
从门厅**四连通**洪水填充（对角挤不过去），再看每一条工位带、每一间房**四条边
各自正对着的方向**上有没有走得到的地方。走不到的方案往后压（一处 600 分）。

**判据是「过得去人」，不是「够 1200」** —— 实测 ≤900 判堵、≥1050 放行。
想卡准 1200 做不到：障碍按格往外取整（房间之间 100 的隔墙全靠这个才不漏），
一条缝最多被吃掉两格，阈值再紧一格连 1200 都会被判堵。1200 那个设计标准由
「次通道」那一栏保证，这一步管的是别出死路。

机房（IT）、清扫间、备餐间、餐厅（茶水间）这四间**默认就是拆掉的**（红色虚框「已拆」）——
甲方点名可拆，他们自己的 PLAN A／B 也已经在拆，留着反而是特例。
**想留哪间，在图上点一下就回来**（变成灰色「可拆」），再点一下又拆掉。

四间全拆腾出 **42.9 ㎡**：

| | 面积 | 采光 |
|---|---|---|
| 东南区 1（原机房＋清扫＋备餐） | 17.0 ㎡ | 内区，无外窗 |
| 东南区 2（原餐厅） | 25.9 ㎡ | **贴南立面幕墙，落地玻璃** |

第二块是重点 —— 这层楼除北区之外，几乎没有别的有天然采光的地方。
**拆餐厅腾出来的不只是面积，是采光面。**
实测：同一套需求（50 工位 ＋ 大中会议 ＋ 4 间洽谈 ＋ 16 ㎡ 茶水），
四间全拆排满 50 个工位、7 间房全落下；一间不拆只排到 44，还差 2 间房。

> 载入 PLAN A／B 时按它们自己记的来（A／B 只拆了 IT 和清扫间），不套这个默认。

> 甲方自己的 PLAN A／B 已经在拆了：两间 7.5 ㎡ 的洽谈室正好画在原机房和清扫间上；
> PLAN B 还把小会议室东墙推到 X=13552，原餐厅往东缩了 652。

**拆归拆，跟客房之间那道墙留着。** 原餐厅的东墙 X=17700 就是客房 01 的西墙 ——
拆的是自己那几间的墙，不是跟别人共用的那道。腾出来的可变区到 X=17550 就停，
留 150 隔墙。北边挨着客房走道的那块同理。

### 路子 A：按需求生成（最快）

右上角「按需求生成」，填：

```
工位数 50      桌面 1500×750
大会议 14 人   中会议 12 人
洽谈 4 间      每间 4 人
茶水间 16 ㎡   打印储物 0 ㎡   电话亭 0 个
主通道 1800    次通道 1200     每段最多 6 桌
偏好 均衡
```

点**生成排布**。它不是按一条固定规则排，而是**把几百种分法全排一遍再挑**：

1. 每块可变区各种切法 —— 整块给工位、整块给房间、西头／东头 3～12 m 给房间、
   南边 3～6 m 给房间 —— 做笛卡尔积，几十到几百种
2. 每一种都真排：房间用装箱算法落位（柱子可以砌进墙里，不能杵在房间中间；
   第二排房间前面自动留一条次通道，门开得出去），工位带横竖各试一遍留多的
3. 工位带**横排竖排都是待选项**，不是先按工位数挑掉一个 —— 朝向要进打分才选得对
4. 北区和南区都排工位时，多一个**「打通」**的待选项 —— 见下
5. 工位带**避柱**：桌面和椅子那 900 都不压柱；每 6 张桌断一条 1200 次通道，全区对齐；
   幕墙前留 1200 采光通廊；带尾只剩一张桌深就铺单排
6. 给每种打分，挑最高的。分数里有：工位数（差一个比多一个疼 4 倍）、
   幕墙 6 m 内的工位比例（中间隔着房间不算）、房间占掉多少采光区、
   最远疏散距离（直线，上限 27.5 m）、几根柱子落在通行区里、有没有房间没落下、
   **几个工位正对／背对幕墙（眩光）**、**房间挡掉了多少幕墙**、
   **从门厅走不到的地方**（这一项太贵，只给排在前面的 60 版算，被罚下去之后循环补算）

右边报：用的是哪种分法（含每块地是横排还是竖排）、工位几个（还能多排几个）、
采光比例、最远疏散、柱子避没避开、几个工位有眩光、每间房尺寸。
**排不下会直说**，不会硬塞。

### 北区南区能不能连成一片

能。**y=10700 那条线是原宿舍隔墙，本来就在要拆的清单里** —— 底板把北区切到
10652、南区从 10751 起，只是为了分开记面积，不是一道留着的墙。

两块地都排工位的时候，多一个「**北区+南区打通**」的待选项：并成一块 18 m 深的
连续场地，工位带一路从北立面排到南墙，中间靠「每段最多几桌」断出来的次通道分层。
甲方 PLAN A 就是这么排的 —— 它有三条带 y 7250~12850，正好横穿这条线。

> 实测：1400×700、不设房间、四间全拆，打通之前上限 103 位，打通之后 **116 位**。

并不并由打分定，两种都在「其他排法」里。**注意打通之后南区那条东西向主通道就没了**
（工位带直接穿过去），循环体系变成「纵向为主」。想保留那条主通道就选不打通那版。

### 走廊怎么留

两条路子，分工不同：

**手钉（＋走廊）** —— 用来定死你要的那条轴，比如「南区主通道必须 1800、必须在
Ⓑ 轴上」。钉完生成器把它当禁建区绕开，存进方案文件、白模里也按通道铺地。
你不钉它也会留出通道（工位带之间 1800、房间之间按「次通道」那一栏），
只是位置由算法定。

**校核（动线开关）** —— 生成完把真正走得通的那片地染出来。这是事后验证，不是你画的：
从门厅出发人能走到哪儿，一目了然。工位带之间那 900 的座椅区太窄，不会被染绿 ——
绿的那片才是真能过人的。

> 只手钉不校核，你等于替生成器干活；只校核不手钉，你没法表达「我要这条轴」。

### 房间能不能贴着窗放

能，但要付代价，而且**按占掉这片立面的比例算，不按米数算**：

| 立面 | 总长 |
|---|---|
| 北 | 16.2 m |
| 南 | 16.2 m |
| **西** | **6.3 m** |

同样挡 2.7 m，在北立面是 17%，在西立面就是 **43%** —— 一间 7.3 ㎡ 的 4 人洽谈室
吃掉全楼西向采光的将近一半。再按这间房服务多少人打折：14 人大会议室占窗还说得过去，
4 人洽谈室很亏，打印储物电话亭占窗等于白扔（这三类本来就先往暗角放）。

**罚款过 15% 就陡起来**（斜率 ×4）。一开始按比例线性罚，结果被 2 个工位买走了半面
西窗 —— 目标差一个扣 40 分，两个 80 分，比罚款还重。占一片立面 15% 以内算正常
（总得放点东西），超过就不是「用了点窗」而是「把一面短立面堵死了」，得另算。

右边会报「房间挡窗：北 17%　南 16%」，超过 25% 标红。

> 不是一刀切禁止。茶水间贴南窗、大会议室贴北窗都是合理的 —— 有窗的会议室比闷罐子强。
> 卡的是「一间小房间吃掉一整面短立面」这种。

### 工位带的朝向是怎么定的

**不是按工位数定的，是按眩光定的。** 到窗多远跟带的方向没关系；方向决定的是
**人对着窗坐还是侧对窗坐** —— 正对幕墙一脸光，背对幕墙屏幕上一片反光，
侧对才对。所以打分里数「视线轴和幕墙法线轴重合」的工位，8 m 以内才算。

北区整块（18.0 × 10.2 m）实测：

| | 工位 | 幕墙 6 m 内 | 眩光 |
|---|---|---|---|
| 横排（背靠背沿 X） | 62 | 50 | **36** |
| 竖排（背靠背沿 Y） | 56 | 40 | **17** |

横排多 6 个工位、多 10 个靠窗，但有 36 个人正对或背对北窗。默认这层楼会选竖排。
**两种都在「其他排法」里列着，数字摆在一起，你自己挑。**
（西立面也有玻璃，所以贴西墙那几列竖排反而有眩光 —— 优化器一般把房间放在西头挡掉。）

**「其他排法」**列着得分靠前的几种，点一下就换 —— 比如「48 位·全房间落下」和
「50 位·少一间洽谈」两种你自己挑。

**偏好**就是换一组权重：

| | 什么时候用 |
|---|---|
| 均衡 | 默认 |
| 采光优先 | 宁可少几个工位也要都靠窗 |
| 数量优先 | headcount 是硬指标 |
| 房间优先 | 会议配套一间都不能少 |

> 报 48／目标 50 就是真差两个。想加：桌面 1500×750 降到 1400×700、
> 「每段最多」调大（少断几条次通道）、或者把餐厅拆了（＋25.9 ㎡ 采光面）。
> 不设房间、四间全拆，1400×700 能排到 117。

### 路子 B：手动画

- **＋ 房间** → 在画布上拖一个矩形 → 右边下拉选名字
- **＋ 工位带** → 拖一条带子，自动铺满工位，右边报几个位
- **＋ 走廊** → 拖一条通道钉死。**房间和工位都会绕开它**，白模里按通道材质铺地。
  右边报净宽：<1200 过不去、1200~1800 次通道、≥1800 主通道
- **选择** → 点中拖着移动，拖右下角改大小，Del 删掉
- **转 90°** → 选中之后，块的**右上角会冒出一个 ↻ 图标**，点它（或按 `R`）。
  整条带（或整间房、整条走廊）绕中心转，框和朝向一起转，椅子跟着走
- **动线** 勾上 → 把从门厅出发、净宽 ≥1200 走得到的地方染成绿色。
  通道连不连、哪儿被卡细了，一眼看得出来
- 吸附 50 mm

工位带的规则：带够深就自动排成背靠背两排（中间留 1800 座椅通行）；
只有一张桌深就排单排（靠墙、靠茶水台那种），这时右边多一个**「椅子在」**下拉 ——
靠墙那条得让人背对墙坐，别选错了。

拖出来的形状决定新建时是横排还是竖排（拖得高过宽就是竖排），画完还能改或者转 90°。
画布上每张桌外侧那个小圆点就是椅子，人坐哪边一眼看得出来。

### 路子 C：从已有图纸导入

甲方给了 PDF 的话，先在机器上跑：

```bash
python3 分析/plan_pdf.py --check    # PDF → 毫米坐标，并出一张套图核对
python3 分析/seed_scheme.py         # → schemes/A.json、B.json
```

然后在排布页「载入…」里选，拖着改。

> 导入是**草稿＋人工确认**，不指望全自动 —— CAD 导出的 PDF 里墙、家具、
> 填充线混在一起没有图层。跑完会打一张面积表，拿它对图上标的 SQM，
> 对不上的手改一条就行。PLAN A／B 就是这么弄准的：去掉两处误判
> （把会议桌、圆桌当成了工位），补上两条单排工位带，工位数才对上 52P。

### 存起来

填个方案号（一个字母或数字，比如 `C`）→ **存进仓库**。
存的是 `schemes/C.json`，只有房间、工位带和拆了哪几间（`demolish`），几十行。

也可以「导出文件」自己拿走。

---

## 二、让新方案能出效果图

存完方案还只是平面。要出 AI 效果图，得先生成白模和底图 —— 这一步要跑 Blender，
只能在机器上跑：

```bash
git pull
bash 分析/build_scheme.sh C          # 约 10 分钟
git add -A && git commit -m "方案 C" && git push
```

它会产出：

```
models/model_C_clay.json             浏览器里的白模（吊顶 3.0 m）
models/model_C_bare.json             裸顶 4.28 m 那版
schemes/C/clays/01~06.png            AI 底图
schemes/C/bares/01~06.png            裸顶底图
schemes/C/lines/01~06.png            线稿
schemes/C/lines_bare/01~06.png
```

**机位不用自己摆。** 脚本按这一版的几何自动找 6 个：射线打不到墙、
视锥里 14 m 内至少 4 张桌、彼此位置差 5 m 朝向差一个大角度。
名字也自动起，比如「北区·西端东南望」。

最后在 `index.html` 的方案下拉里加一行 `<option value="C">…</option>`。

要把这一版也放进**方案研究**页对照的话，先出一张跟那几张同图例的平面：

```bash
python3 分析/draw_json.py C          # → 分析/plate_C.svg
```

再把 SVG 贴进 `plan.html` 的 tab 里（照着 `pane-PA` 那段抄）。

---

## 三、出效果图

打开 **AI 渲染**页。

### 1. 选方案、选视角

导航条上的「方案」下拉切方案。左边一列是这一版的视角，点一个。

### 2. 选底图

| 选项 | 什么时候用 |
|---|---|
| **白模 · 吊顶 3.0 m** | 默认，原设计的净高 |
| **白模 · 裸顶 4.28 m** | 想看露结构与机电的效果（净高差 1.28 m，这层楼最值钱的一个决定） |
| 渲染图 | 已带材质，AI 容易保留三维感 —— 一般别选 |
| **自由取景** | 六个预设机位都不满意，在三维里走到任意位置 |
| 上一轮出图 | 拿已经出过的一张再走一轮，只补真实感 |

自由取景：拖＝转头，滚轮＝前后，Shift＋拖＝平移，WASD＝走动。
画布下面会提示构图问题（镜头贴着一块板、画面里没桌子），别浪费额度。

### 3. 定设计

**吊顶**单独一行（这层楼净高差 1.28 m，最值钱的决定），其余是下拉：
窗／遮阳、灯具、地面、家具风格、工位屏风、整体色调、人物。

有想要的效果图/实景照，上传到「风格参考图」→「读取风格」，
AI 会读出它的材质和光感并入到提示词里。

### 4. 出图

- **引擎**：gpt-image-1 或千问。千问便宜，但**不能喂线稿**（它会把线当成真构件画出来，
  整张图变成玻璃盒子），页面上会自动帮你关掉。
- **画质**：medium 够用。high 慢，可能超时。
- 点右下角常驻栏的**生成**。生成中那条会显示服务端等了多少秒。

### 5. 核图 —— 这一步别省

出图后点 **「套白模线稿」**，红线是白模里那一帧的真实轮廓。
AI 是「照着画」不是「按线填色」，几何会漂。重点核三条：

1. 工位数对不对
2. 主通道 1800、次通道 1400 有没有被收窄
3. 幕墙是不是落地玻璃（4050 一格），有没有被画成 1.1 m 窗台的窗洞

不满意就点「拿这张再走一轮」，右下角变成「开始第二轮」，再点一次。

出的图会自动存进仓库 `产出/`，重启也不会丢。

---

## 已知的限制

- **AI 出图的几何会漂。** gpt-image-1 和千问都没有 ControlNet，都是「看着参考图重画」。
  材质光感很像照片，但尺寸不能当方案依据 —— 依据看白模和平面。
- 拆房间只改白模和平面，**机电没跟着改** —— 餐厅拆了，那儿的给排水、排油烟、燃气点位
  怎么处理要另说；机房（IT）拆了，弱电主机挪到哪儿要先定。
- 房间的门开在哪一侧是猜的（往外 400 mm 那点不落在别的房间里、且离通道最近）。
  猜错了白模上一眼能看出来，改方案文件里房间位置即可。
- 梁高不知道 —— DWG 里没有梁的图层，要现场量或者调结构图纸。
